/*
package com.intuit.accountant.services.dcm.resources.helpers;

import com.intuit.accountant.services.helpers.RequestThreadLocal;
import org.apache.commons.lang3.StringUtils;

import javax.ws.rs.container.ContainerRequestContext;
import javax.ws.rs.container.ContainerRequestFilter;
import javax.ws.rs.ext.Provider;
import java.io.IOException;

import static com.intuit.accountant.services.dcm.resources.ResourceConstants.APPLICATION_NAME_HEADER;

@Provider
public class ReminderLogHelperRequestFilter implements ContainerRequestFilter {
    @Override
    public void filter(ContainerRequestContext requestContext) throws IOException {
        String headerString = requestContext.getHeaderString(APPLICATION_NAME_HEADER);
        if (StringUtils.isEmpty(headerString))  {
            headerString = "Unknown";
        }
        RequestThreadLocal.put(APPLICATION_NAME_HEADER, headerString);
    }
}
*/
